VLACORA HUB 0.22.2
Standalone agenda + Social Studio workflow release.

Current backend:
- Supabase Auth
- Supabase PostgreSQL
- Row Level Security

Build fixes 0.22.2:
- Hitlist CSV/PDF export functions remain restored.
- Editorial workspace persistence now returns a typed workspace record instead of a boolean.
- Editorial revision is read from source_revision consistently.
- Social review author lookup now uses Map<string,string> so strict TypeScript accepts author_name.

Still standalone:
- No Rotation/Playout integration code
- No radio API proxy
- No stream/listener statistics integration
- No encoder/playout monitoring

Migrations required for existing 0.21.x databases:
- supabase/migrations/029_standalone_stations.sql
- supabase/migrations/030_calendar_social_workflow.sql
