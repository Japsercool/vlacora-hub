# Gateway in Docker

Voor PULSE 0.31 wordt de Gateway normaal niet meer los op Windows geïnstalleerd. Gebruik `pulse-docker/scripts/INSTALL_PULSE_DOCKER.ps1`. De Gateway draait samen met PostgreSQL in de Compose stack. Los starten is alleen voor ontwikkeling.
